# Root .env — Postgres container credentials
# Copy to VPS: /root/portfolio/.env
# DB_PASSWORD must match backend/.env DB_PASSWORD exactly

POSTGRES_USER=timetable_admin
POSTGRES_PASSWORD=TtDb_2026_Vps_Secure_xK9m
POSTGRES_DB=timetable_db
